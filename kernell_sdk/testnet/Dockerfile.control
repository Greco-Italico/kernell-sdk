FROM python:3.10-slim

WORKDIR /app

# Copy SDK code
COPY . .

RUN pip install --no-cache-dir fastapi uvicorn pydantic redis requests

# Run the control plane
CMD ["uvicorn", "testnet.control_plane:app", "--host", "0.0.0.0", "--port", "8000"]
